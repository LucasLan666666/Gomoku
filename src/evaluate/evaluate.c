// 本文件用于实现对棋盘的打分
#include "../gomoku.h"

// 打分函数，接受棋盘和玩家作为参数，返回一个分数
int evaluate(signed char board[SIZE][SIZE], signed char player) {


    return 0;
}

// 比大小函数，接受两个分数，返回较大的那个
int max(int a, int b) {
    return a > b ? a : b;
}
// 比大小函数，接受两个分数，返回较小的那个
int min(int a, int b) {
    return a < b ? a : b;
}